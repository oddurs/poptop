<!--
  `./check` is what CI runs, in the order it runs it. On a Mac, `./check
  --linux` as well: the /proc backend is not compiled by a macOS build at all.
-->

## What this changes, and why

## How it was checked

<!--
  A test that would fail without the change, where the thing is testable; a
  measurement where it is a cost; the output, where it is something drawn. If a
  number in the documentation is now wrong, it changes in the same commit.
-->

- [ ] `./check` passes
- [ ] A test covers it, or there is a reason here why not
